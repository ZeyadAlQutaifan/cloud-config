spring:
  application:
    name: dg-ms-customer
server:
    port: 8082
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/


management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
      show-details=always


digital:
  url:
    cards: dg-ms-card

