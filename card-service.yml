server:
  port: 8083

spring:
  application:
    name: card-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8768/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    show-details: always

digital:
  test: Hello-test
