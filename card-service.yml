spring:
  application:
    name: dg-ms-card
server:
    port: 8083
eureka:
    client:
        service-url:
            defaultZone: ${SERVICE_REGISTERY_URL}


management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
      show-details=always


digital:
  test: Hello From Config
otel:
  service:
    name: demo               
  exporter:
    otlp:
      endpoint: http://localhost:4317
      protocol: grpc
traces:
  sampler:
    probability: 1.0       
